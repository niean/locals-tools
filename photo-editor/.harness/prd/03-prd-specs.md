# 需求规格

## 约束
本文仅供自然人使用，未经人工确认、禁止AI修改。如人工确认，必须是针对本文件的单独确认，严禁和其它文件一起确认。

## 需求

[20260325]
- FR
    - 启动：启动新项目，实现一个图片编辑器。要求：
        - 本地化的HTML工具，浏览器打开；
        - 支持常见的照片编辑功能，如裁剪(自由、原始比例)等；
        - 技术规范体系：JS使用Airbnb JavaScript Style Guide，CSS使用ITCSS架构分层、BEM命名；
        - 迭代：项目名称 PhotoEditor；
    - 加载：ISSUE，无法加载图片，按钮打开、拖拽图片均无效；
        - 原因：ES6 Modules 需要 HTTP 服务器，不能直接用 file:// 打开；解决：通过 python3 -m http.server 启动本地服务器后访问
        - 迭代：http.server 8888启动，增加一个shell启动脚本，放到项目的上一级目录，设置开机启动(用户确认)
    - 截图：选定范围后，未展示确定或取消按钮，期望正常展示、且可人工拖拽；
        - 迭代：截图，选定范围后、双击选中区域完成确定；
    - 截图：选定范围后，保留当前的下载按钮(实质是另存为)，同时允许直接将变更、保存到原文件；
        - 追加：下载保存时，对话框要展示文件类型后缀；

[20260330]
- FR
    - 启动：将start-locals-http.sh加入到mac的启动项，每次OS启动时自动执行；
    - 截图：比例，默认值从free调整为original；
    - 截图：裁剪，双击选中的图片区域确认保存，按ESC键取消保存，不再需要单独的确认/取消按钮框；
    - 截图：裁剪，选择框定型后支持移动；
    - 旋转：支持图片旋转方向，只需支持逆时针旋转；
        - 迭代：旋转图标，顶部被削掉了，请改小高度或更换图标；
    - 打码：块大小，从数字、改为圆形图标列表，分别代表1、2、5、10；
    - 保存：定义快捷键CMD+S，用于保存图片，如跟浏览器冲突、优先保存图片；

[20260403]
- FR
    - 截图：点击工作区，触发图片选择；
    - 截图：旋转按钮，仅存在于截图页面，去掉其它页面的旋转按钮；

[20260414]
- FR
    - 形状：在箭头的基础上，新增矩形、圆形选项，选择组件从下拉、改为平铺
    - 样式：整个背景色，从Dark调整为Light，对应的图标也同步修改(使能看到)
    - 操作：左侧工具栏，鼠标放上去之后、展示Tips，Tips内容为 裁剪、画笔、文字、马赛克或形状。因为，只看图标、无法确认操作类型
    - 质量：Bug，每次保存，图片像素都会降低、视觉效果变差

[20260415]
- FR
    - 操作：左侧工具栏，支持展开模式，展开后菜单形式为 图标+文字，现状是收起模式、只展示图标
    - 优化：浏览器Tab栏，设置图标
    - 文字：Bug，添加文字框后，无法就地输入、而是需要点一下框；完成文字输入后，文字位置会变一下(下移一点点)、所见非所得
    - 保存：保存时，图片名称自动过滤空字符
    - 滤镜：左侧工具栏新增滤镜功能，支持原片、鲜明、暖色等常用滤镜(参考iPhone图片编辑中的滤镜)，滤镜选择放到顶部属性栏
        - 迭代：滤镜，鲜明、喜剧的效果相似，鲜明需要参考iPhone的样式修改下
        - 迭代：滤镜图标，方改圆、缩小1/3 => 滤镜的属性图标不好看，去掉圆圈内的图标、只保留文字，圆圈填充底色 => 滤镜的属性图标，字体改为白色
        - Bug：裁剪旋转后，切到滤镜项后、图片会因为保持初始方向而截断

[20260610]
- FR
    - 视频：所有操作，支持视频

---
[待办]
- ISSUE
    - 视频：编辑，切换方向后，视频超出了编辑框、有一部分看不到了
- FR
    - 模式：批量模式，一次选中和加载多张图片，之后独立的编辑和保存
- NFR
    - HE：上移到tools目录

